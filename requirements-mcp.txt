# Extra dependency for the local MCP server (mcp_server.py).
# Kept separate from requirements.txt so the privacy-first web app stays a lean
# install; only users who run the MCP server need this.
#
#   pip install -r requirements.txt -r requirements-mcp.txt
mcp>=1.27.2
